.col-md-10.col-md-offset-1
  .edit-review-form
    %h1 Edit Review
    = simple_form_for([@play, @review]) do |f|
      #rating-form
        %label Rating
      = f.input :comment
      = f.button :submit, class: 'btn-custom2'
    -# = simple_form_for(@play) do |f|
    -#   = simple_fields_for(@review) do |r|
    -#     #rating-form
    -#       = r.input :rating
    -#     = r.input :comment
    -#     = r.button :submit, class: 'btn-custom2'

:javascript
  $('#rating-form').raty({
    path: '/assets/',
    scoreName: 'review[rating]'
  });