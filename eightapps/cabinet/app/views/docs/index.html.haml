.wrapper_with_padding
  #docs.clearfix
    - if @docs.present?
      - @docs.each do |doc|
        %a{href: (url_for [doc])}
          .doc
            %p.title= doc.title
            %p.data= time_ago_in_words(doc.created_at)
            %p.content= truncate(doc.content, length: 50)
    - else
      %h2 Create %Doc!
      %p Start Creating Documents and Organizing Your Life!
      %button= link_to 'Create Doc', new_doc_path
